# aggregator/

Scripts that source WordPress exploit PoCs (curated list + optional
Exploit-DB/GitHub Advisory lookups) and replay them against the sandbox
mock listener so their raw HTTP traffic gets captured into Postgres.
