# features/

Feature engineering: heuristic extractors (entropy, char ratios, PHP/SQL
token flags) and the TF-IDF char n-gram vectorizer, plus the adversarial
augmentation engine (URL encoding, double encoding, SQL comment-token
mutation, PHP wrapper mutation).
